name: ci

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      # Data code is tested with the same rigor as application code: the full
      # pipeline runs, then the unit + SQL-equivalence suite gates the build.
      - name: Generate deterministic dataset
        run: python -m src.generate_data

      - name: Generate ERP / MES / QMS source-system extracts
        run: python -m src.generate_systems

      - name: Generate station self-test records (reference coupon + tooling inspection)
        run: python -m src.generate_selftest

      - name: Run pipeline (validation -> SPC -> capability -> marts)
        run: python -m src.pipeline_pandas

      - name: Integrate heterogeneous systems (ERP + MES + QMS joins)
        run: python -m src.integrate

      - name: AI root-cause triage
        run: python -m src.ai_triage

      - name: Audit inspection coverage + position-dependent operating point
        run: python -m src.coverage

      - name: Build scorecard + interactive dashboard + integration + coverage pages
        run: |
          python -m src.scorecard
          python -m src.dashboard
          python -m src.integration_page
          python -m src.coverage_page

      - name: Run tests
        run: pytest -q

      - name: Publish dashboards
        uses: actions/upload-artifact@v4
        with:
          name: quality-dashboards
          path: output/
